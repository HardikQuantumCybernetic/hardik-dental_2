project_id = "mmsmljkeedqfrbgsqipf"

[functions.chat-with-gemini]
# SECURITY FIX: Enable JWT verification for authenticated access
# This ensures only authenticated users can access the chatbot
verify_jwt = true
